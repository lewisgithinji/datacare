import Chatbot from "@/components/Chatbot";

/**
 * This page serves as a container for the marketing website's chatbot analytics.
 * It allows access to the Chatbot component at a dedicated URL.
 */
const MarketingChatbotAnalytics = () => {
  return <Chatbot />;
};
export default MarketingChatbotAnalytics;